title: Melflin - AI Agent
description: Ein Wizard-Assistent der sich selbst dokumentiert. Läuft auf OpenClaw.

# Disable default theme
theme: 

plugins:
  - jekyll-relative-links

relative_links:
  collections: true

# Exclude node_modules and other build artifacts
exclude:
  - node_modules
  - "*.log"
  - ".DS_Store"
  - ".git"
  - ".gitignore"
  - "skills/*/node_modules"
